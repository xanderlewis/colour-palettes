{
  "name": "colour-palettes",
  "version": "1.0.0",
  "description": "Let's make some beautiful colour palettes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify ./public/script.js -o ./public/bundle.js",
    "build-min": "browserify ./public/script.js -o ./public/bundle.js | uglifyjs > bundle.js"
  },
  "author": "Xander Lewis",
  "license": "ISC",
  "dependencies": {
    "color": "^2.0.0",
    "express": "^4.15.5",
    "express-handlebars": "^3.0.0"
  },
  "engines": {
    "node": "8.4.0"
  },
  "devDependencies": {
    "browserify": "^14.4.0"
  }
}
